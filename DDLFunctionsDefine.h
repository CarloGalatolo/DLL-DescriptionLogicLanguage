#define HASHSIZE 101

char* loadedDB;
int isLoaded;

void myToUpper (char*);

void setLoadedDB (char*);

void creaDB(char*);
void creaTable(char*);

int rimuoviDB(char*);

void appendAttr (char*, char*);
